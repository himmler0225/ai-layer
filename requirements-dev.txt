# Exact-pinned versions — see requirements.txt for the upgrade policy.

# Alembic migrations — SQLAlchemy schema
alembic==1.18.5

# Testing
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.0.0

# Linting (matches CI)
ruff==0.15.20
