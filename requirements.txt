# Web framework & server (uvicorn, httpx, pydantic-settings via extras)
fastapi[standard]>=0.128.0

# Validation & config
pydantic>=2.11.0
python-dotenv>=1.1.0
jsonschema>=4.0.0

# HTTP & OpenAI
httpx>=0.28.0
openai>=1.40.0

# Rate limiting
slowapi>=0.1.10

# PostgreSQL — SQLAlchemy async + asyncpg + pgvector
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30.0
pgvector>=0.3.6

# Redis — history cache + rate-limit storage (optional)
redis>=5.3.0

# GeoIP — optional local DB when GEOIP_DB_PATH is set
geoip2>=4.8.0

# MCP client (data-miner crawl tools)
mcp[cli]>=1.0

# Langgraph
langgraph>=0.2.0
langchain-core>=0.3.0